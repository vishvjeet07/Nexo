<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nexo</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="icon" href="../../image/favicon.ico" type="image/x-icon">
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between px-5 py-3 ">
        <h3 class="text-xl">Nexo</h3>
        <% var loggedin =  typeof loggedin !== "undefined" ? loggedin : true %>
        <% if(loggedin){ %>
        <div class="flex gap-5">
            <a href="/shop">Shop</a>
            <a href="/cart">Cart</a>
            <a href="/account">My Acoount</a>
            <a class="text-red-600" href="/users/logout">Logout</a>
        </div>
        <% } %>
    </nav>
    <div class="bg-white rounded-lg shadow-lg m-auto mt-36 p-6 w-full max-w-sm text-center">
        <h1 class="text-xl font-bold text-gray-800 mb-4">Profile</h1>
        <p class="text-gray-800 text-lg mb-2">
            <span class="font-medium text-gray-600">Name:</span> <%= user.fullname %>
        </p>
        <p class="text-gray-800 text-lg">
            <span class="font-medium text-gray-600">Email:</span> <%= user.email %>
        </p>
    </div>